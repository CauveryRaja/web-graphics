const canvasElm = document.getElementById('canvas');
const cxt = canvasElm.getContext('2d');

if(!cxt) {
    throw 'Canvas context not available!'
}

// Draw snake

// Animate snake

// Add Keyboard Controls